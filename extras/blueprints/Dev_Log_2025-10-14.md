# OpenCode Trainer – Dev Log (2025‑10‑14)

Scope: Lineage, export pipeline, promotion gating, Training UI and Models Overview refinements.

Summary
- Fixed Models → Ollama list layout: Assigned/Unassigned expand under their headers.
- Evaluation header: added Parent line, class chip, and “Lineage: <ULID‑short>”.
- Collections/Assigned integration: selecting a Variant now sets eval override to its assigned GGUF; selecting a GGUF backfills type defaults and parent.
- ULID lineage core: ensure_lineage_id for variants; lineage on sidecars and eval metadata.
- Assignments v2: tag_index reverse lookup + helpers; legacy‑compatible.
- Training UI:
  - Model Selection: new Variant combobox under Base; auto‑selects on Apply; updates Runner header.
  - Runner header: class chip + Lineage short; “Novice mode” cue when no adapters exist.
  - Profiles tab: Set/Clear Default Profile; default auto‑applied silently; legacy “Profile Loaded” popups removed.
- Models → Overview:
  - Lineage section lists Variants (Collections) and Assigned GGUF tags (live detection); class chips; clickable shortcuts to open Variant or GGUF.
- Levels manifests:
  - New saves: variant_id/lineage_id/assigned_type/class_level when derivable.
  - Legacy backfill on read using adapter sidecars.
- Export pipeline (Level → GGUF):
  - Prefer manifest lineage/variant for tag naming: `<variant_id>:<quant>`.
  - One‑GGUF gating by lineage with replace prompt; assignment registered; manifest exports[] updated.
- Promotion gating:
  - “Promote to Skilled” button (disabled until earned) with tooltip showing metrics.
  - One‑time alerts when promotion unlocked on Variant/Base selection.
  - Promotion action creates Skilled variant profile + training profile, preserves sticky prompts/schemas.

Implementation notes
- Lineage helpers in `Data/config.py`: `ensure_lineage_id`, `get_lineage_id`, `get_assigned_tags_by_lineage`, `get_lineage_for_tag`, legacy‑safe `add_ollama_assignment`.
- Sidecars now include `lineage_id`; profiles enriched with `adapters[]` and `latest_adapter` on save.
- Eval metadata includes `lineage_id` (variant or resolved from GGUF tag).
- One‑GGUF Create button gating + tooltip; assignment cleanup on delete.
- Lineage Overview section renders live state with chips and interactive buttons.
- Export uses manifest’s lineage; updates assignment and manifest exports; refreshes models list.
- Promotion thresholds (current): Overall Δ ≥ +5.0; JSON ≥ 95%; Schema ≥ 95%; no regressions.

Key files touched
- Models Tab: `Data/tabs/models_tab/models_tab.py`
- Training UI: `Data/tabs/training_tab/model_selection_panel.py`, `runner_panel.py`, `profiles_panel.py`, `training_tab.py`
- Engine: `Data/training_engine.py`, `Data/evaluation_engine.py`
- Config: `Data/config.py`
- Plan: `extras/Plans/ULID_Lineage_and_Promotion_Plan_2025-10-14.md`

Next (suggested)
- Complete Promotion flow: optional immediate Skilled export + assignment.
- History lineage grouping and badges (Runs/Levels).
- Optional: lineage/class badges inside Variant combobox entries.

